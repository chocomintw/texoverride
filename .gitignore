# build artifacts (attach the built .asi to a GitHub Release instead of committing it)
*.asi
*.obj
*.res
*.exp
*.lib
*.log

# working files kept in the project folder on purpose (user preference: scratchpads live with
# the project, not in %TEMP%). Multi-GB extracted game assets, throwaway tools, downloaded logs.
scratch/
temp_converting/
texoverride.zip

# private working notes, never pushed
CLAUDE.md
